pkgname=tanx-deps-hypr
pkgver=1.0.0
pkgrel=1
pkgdesc="Dependencies for tanx hypr config"
arch=('any')
depends=(
  "hyprland"
  "hypridle"
  "hyprcursor"
  "xdg-desktop-portal-hyprland"
  "wl-clipboard"
  "ntfs-3g"
)

package() {
  # Meta-package: no files installed
  return 0
}
